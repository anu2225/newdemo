खूप छान 👌 आता आपण अगदी step-by-step practically तुझा Hotel Module (Frontend + Backend) नीट चालू करणार आहोत — मी तुला एकदम simple language मध्ये सांगतो.

तुझ्या zip मध्ये सगळं आहे (Angular frontend + Spring Boot backend), फक्त काही जागी छोटे connections set करणं बाकी आहे — तेच आपण करूया.


---

🧩 Step 1 — Backend नीट चालू कर

1. MySQL उघड.

CREATE DATABASE hotelDB;


2. जा इथे:

backend/smarthotelbooking/src/main/resources/application.properties

DB username/password बदल (तुझ्या मशीनप्रमाणे):

spring.datasource.url=jdbc:mysql://localhost:3306/hotelDB
spring.datasource.username=root
spring.datasource.password=YOUR_PASSWORD
spring.jpa.hibernate.ddl-auto=update
server.port=8080


3. Run backend:

cd backend/smarthotelbooking
mvn spring-boot:run


4. Browser मध्ये बघ:

http://localhost:8080/hotels

(जर JSON दिसलं, तर backend ready ✅)




---

⚙️ Step 2 — Angular frontend configure कर

Frontend path:

frontend/SmartHotelBookingSystem/

1. Terminal उघड:

cd frontend/SmartHotelBookingSystem
npm install


2. आता backend URL जोड: जा 👉

src/environments/environment.ts

(जर हा file नसेल, तयार कर.)

export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080'
};

आणि prod साठी:

src/environments/environment.prod.ts

export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:8080'
};


3. मग HotelService तयार कर (किंवा edit कर):
👉 path: src/app/core/services/hotel.service.ts

import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { environment } from '../../../environments/environment';

@Injectable({ providedIn: 'root' })
export class HotelService {
  private base = `${environment.apiBaseUrl}`;

  constructor(private http: HttpClient) {}

  getAllHotels() {
    return this.http.get(`${this.base}/hotels`);
  }

  getHotelById(id: number) {
    return this.http.get(`${this.base}/hotels/${id}`);
  }

  addHotel(hotel: any) {
    return this.http.post(`${this.base}/hotels`, hotel);
  }

  updateHotel(id: number, hotel: any) {
    return this.http.put(`${this.base}/hotels/${id}`, hotel);
  }

  deleteHotel(id: number) {
    return this.http.delete(`${this.base}/hotels/${id}`);
  }
}


4. मग component मध्ये वापर: 👉 path: src/app/modules/hotel/hotel-list.component.ts

import { Component, OnInit } from '@angular/core';
import { HotelService } from 'src/app/core/services/hotel.service';

@Component({
  selector: 'app-hotel-list',
  templateUrl: './hotel-list.component.html',
})
export class HotelListComponent implements OnInit {
  hotels: any[] = [];

  constructor(private hotelService: HotelService) {}

  ngOnInit() {
    this.loadHotels();
  }

  loadHotels() {
    this.hotelService.getAllHotels().subscribe({
      next: (res) => (this.hotels = res),
      error: (err) => console.error('Error loading hotels', err),
    });
  }
}

आणि त्याच्या HTML मध्ये (hotel-list.component.html)

<div class="container">
  <h2>Hotel List</h2>
  <table>
    <thead>
      <tr><th>ID</th><th>Name</th><th>City</th><th>Actions</th></tr>
    </thead>
    <tbody>
      <tr *ngFor="let h of hotels">
        <td>{{h.id}}</td>
        <td>{{h.name}}</td>
        <td>{{h.city}}</td>
        <td>
          <button (click)="delete(h.id)">Delete</button>
        </td>
      </tr>
    </tbody>
  </table>
</div>




---

🧠 Step 3 — Run frontend

ng serve --open

Browser:
👉 http://localhost:4200
जर backend 8080 वर चालू असेल, तर आता hotel list दिसायला पाहिजे ✅


---

🔧 Step 4 — Optional (Proxy file for CORS)

जर काही error आला CORS चा, तर Angular root मध्ये proxy.conf.json तयार कर:

{
  "/": {
    "target": "http://localhost:8080",
    "secure": false,
    "changeOrigin": true
  }
}

आणि मग चालवा:

ng serve --proxy-config proxy.conf.json


---

✅ Step 5 — Test

1. Backend logs पहा (Spring console वर queries दिसतात).


2. Angular network tab मध्ये check करा की request http://localhost:8080/hotels ला जातोय का.


3. JSON डेटा browser वर दिसतोय का.




---

असं केल्यावर तुझं hotel module (frontend + backend) proper connect होईल आणि add/update/delete सर्व नीट चालतील 🔥


---

हवं असल्यास मी आता ready-to-use hotel.service.ts + environment files + proxy.conf.json + test curl commands असं full pack तयार करून देतो (copy-paste ready).
करू का?

